

export enum Role {
    ADMIN = "Admin",
    SALER = "Saler",
    USER = "User",
}

export enum PaymentStatus {
    CANCELLED = "CANCELLED",
    PENDING = "PENDING",
    SUCCESS = "PAID",
    EXPIRED = "EXPIRED"
}